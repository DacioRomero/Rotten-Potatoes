<!-- views/reviews-edit.handlebars -->

<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <form method="POST" action="/movies/{{review.movieId}}/reviews/{{review._id}}?_method=PUT">
            {{> reviews-form movieId=review.movieId }}
        </form>
    </div>
</div>
