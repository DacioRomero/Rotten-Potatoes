<!-- views/reviews-new.handlebars -->

<form method="POST" action="/reviews/{{review._id}}?_method=PUT">
    {{> reviews-form}}

    <!-- BUTTON -->
    <p>
        <button type="submit">Save Review</button>
        <button type="button" onclick="window.location='/reviews/{{review._id}}'">Cancel</button>
    </p>

</form>
